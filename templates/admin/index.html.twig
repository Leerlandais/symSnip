{% extends 'template.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block sideBar %}
<aside class="bg-gray-800 text-white w-64 min-h-screen p-4">
    <nav>
        <ul class="space-y-2">
    <li class="option-with-dropdown-inner">
        <a href="{{ path('admin_add_code') }}">
            <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                <div class="flex items-center">
                    <span>Add Code</span>
                </div>
            </div>
        </a>
        <a href="{{ path('admin_add_html') }}">
            <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                <div class="flex items-center">
                    <span>Add HTML</span>
                </div>
            </div>
        </a>
    <a href="{{ path('admin_add_code') }}">
        <div class="flex items-center justify-between p-2 hover:bg-gray-700">
            <div class="flex items-center">
                <span>Add Executable</span>
            </div>
        </div>
    </a>
    </li>

    <li class="option-with-dropdown">
        <div class="flex items-center justify-between p-2 hover:bg-gray-700">
            <div class="flex items-center">
                <i class="fas fa-file-alt mr-2"></i>
                <span>Update</span>
            </div>
            <i class="fas fa-chevron-down text-xs"></i>
        </div>
        <ul class="dropdown ml-4 hidden">
            <li class="option-with-dropdown-inner">
                <a href="{{ path('admin_add_code') }}">
                    <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                        <div class="flex items-center">
                            <span>Code</span>
                        </div>
                    </div>
                </a>
            </li>
            <li class="option-with-dropdown-inner">
                <a href="{{ path('admin_add_code') }}">
                    <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                        <div class="flex items-center">
                            <span>Html</span>
                        </div>
                    </div>
                </a>
            </li>
        </ul>
    </li>
        </ul>
    </nav>
    <a href="https://leerlandais.com/portail" class="bottom-40 absolute bg-blue-500 py-2 px-6 mt-2 rounded-md text-white w-auto ml-12 font-bold cursor-pointer hover:bg-green-600"><button>Portfolio</button></a>
</aside>
{% endblock %}
{% block content %}
    <div class="container pt-8 mt-6 flex flex-col gap-4">
        <h2 class="text-5xl">Welcome</h2>
        <h3 class="text-4xl">Use the options on the sidebar to navigate to the code</h3>
        <h4 class="text-2xl">&lpar;This site was created using Symfony&rpar;</h4>
        <div class="container pt-8 mt-6 flex flex-col gap-4">

    </div>
    </div>

{% endblock %}
