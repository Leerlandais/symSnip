{% extends 'template.back.html.twig' %}

{% block title %}Hello AdminController!{% endblock %}

{% block sideBarAdmin %}
    <li class="option-with-dropdown-inner">
        <a href="{{ path('app_admin_code_new') }}">
            <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                <div class="flex items-center">
                    <span>Add Code</span>
                </div>
            </div>
        </a>
        <a href="{{ path('app_admin_html_new') }}">
            <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                <div class="flex items-center">
                    <span>Add HTML</span>
                </div>
            </div>
        </a>
        <a href="{{ path('app_admin_exes_new') }}">
            <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                <div class="flex items-center">
                    <span>Add Executable</span>
                </div>
            </div>
        </a>
    </li>

    <li class="option-with-dropdown">
        <div class="flex items-center justify-between p-2 hover:bg-gray-700">
            <div class="flex items-center">
                <i class="fas fa-file-alt mr-2"></i>
                <span>Update</span>
            </div>
            <i class="fas fa-chevron-down text-xs"></i>
        </div>
        <ul class="dropdown ml-4 hidden">
            <li class="option-with-dropdown-inner">
                <a href="{{ path('app_admin_code_index') }}">
                    <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                        <div class="flex items-center">
                            <span>Code</span>
                        </div>
                    </div>
                </a>
            </li>
            <li class="option-with-dropdown-inner">
                <a href="{{ path('app_admin_html_index') }}">
                    <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                        <div class="flex items-center">
                            <span>Html</span>
                        </div>
                    </div>
                </a>
            </li>
            <li class="option-with-dropdown-inner">
                <a href="{{ path('app_admin_exes_index') }}">
                    <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                        <div class="flex items-center">
                            <span>Executables</span>
                        </div>
                    </div>
                </a>
            </li>
        </ul>
    </li>

{% endblock %}