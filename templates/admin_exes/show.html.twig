{% extends 'base.html.twig' %}

{% block title %}ExesCode{% endblock %}

{% block body %}
    <h1>ExesCode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ exes_code.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ exes_code.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ exes_code.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ exes_code.image }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ exes_code.location }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_exes_index') }}">back to list</a>

    <a href="{{ path('app_admin_exes_edit', {'id': exes_code.id}) }}">edit</a>

    {{ include('admin_exes/_delete_form.html.twig') }}
{% endblock %}
