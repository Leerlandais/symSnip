{% extends 'base.html.twig' %}

{% block head %}
    {{ parent() }}

    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

    {% block stylesheets %}
        <script src="https://cdn.tailwindcss.com"></script>
        <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
        <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet">
    {% endblock %} {# end stylesheets #}
{% endblock %} {# end head #}



{% block mainClass %} 'flex flex-col text-center mx-auto w-fit' {% endblock %}
{% block bodyClass %}"bg-gray-200"{% endblock %}
{% block header %}

    <nav class="bg-blue-500 p-4 flex items-center justify-between">
        <div>
            <a href="{{ path('app_main') }}"><h1 class="text-white text-xl font-semibold">Code Snippets</h1></a>

        </div>
        <div class="flex items-center space-x-4">
            {% block headerLink %}
                {% if is_granted('IS_AUTHENTICATED') %}
                        <div>
                            <a href="{{ path('app_admin') }}"><h1 class="text-white text-xl font-semibold">Controls</h1></a>
                        </div>
                    <a href="{{ path('app_logout') }}"><span class="text-white">Logout</span></a>
                {% else %}
                    <a href="{{ path('app_login') }}"><span class="text-white">Login</span></a>
                {% endif %}
            {% endblock %}
            <i class="fas fa-user-circle text-white text-2xl"></i>
        </div>
    </nav>
{% endblock %} {# end header #}


{% block sideBar %}
    <aside class="bg-gray-800 text-white w-64 min-h-screen p-4">
        <nav>
            <ul class="space-y-2">

                {% block sideBarPublic %}
                    <li class="option-with-dropdown">
                        <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                            <div class="flex items-center">
                                <i class="fas fa-file-alt mr-2"></i>
                                <span>PHP</span>
                            </div>
                            <i class="fas fa-chevron-down text-xs"></i>
                        </div>
                        <ul class="dropdown ml-4 hidden">
                            <li class="option-with-dropdown-inner">
                                <a href="{{ path('app_type', {type: 'phpCall' }) }}">
                                    <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                                        <div class="flex items-center">
                                            <span>Calls</span>
                                        </div>
                                    </div>
                                </a>
                            </li>
                            <li class="option-with-dropdown-inner">
                                <a href="{{ path('app_type', {type: 'phpFunc' }) }}">
                                    <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                                        <div class="flex items-center">
                                            <span>Functions</span>
                                        </div>
                                    </div>
                                </a>
                            </li>
                        </ul>
                    </li>
                    <li class="option-with-dropdown">
                        <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                            <div class="flex items-center">
                                <i class="fas fa-file-alt mr-2"></i>
                                <span>Javascript</span>
                            </div>
                            <i class="fas fa-chevron-down text-xs"></i>
                        </div>
                        <ul class="dropdown ml-4 hidden">
                            <li class="option-with-dropdown-inner">
                                <a href="{{ path('app_type', {type: 'java' }) }}">
                                    <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                                        <div class="flex items-center">
                                            <span>Main</span>
                                        </div>
                                    </div>
                                </a>
                            </li>
                            <li class="option-with-dropdown-inner">
                                <a href="{{ path('app_type', {type: 'xtra' }) }}">
                                    <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                                        <div class="flex items-center">
                                            <span>Xtra</span>
                                        </div>
                                    </div>
                                </a>
                            </li>
                        </ul>
                    </li>
                    <li class="option-with-dropdown">
                        <a href="{{ path('app_html') }}">
                            <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                                <div class="flex items-center">
                                    <i class="fas fa-file-alt mr-2"></i>
                                    <span>HTML</span>
                                </div>
                            </div>
                        </a>
                    </li>
                    <li class="option-with-dropdown">
                        <a href="{{ path('app_type', {type: 'unix' }) }}">
                            <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                                <div class="flex items-center">
                                    <i class="fas fa-file-alt mr-2"></i>
                                    <span>Linux</span>
                                </div>
                            </div>
                        </a>
                    </li>
                    <li class="option-with-dropdown">
                        <a href="{{ path('app_type', {type: 'reac' }) }}">
                            <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                                <div class="flex items-center">
                                    <i class="fas fa-file-alt mr-2"></i>
                                    <span>React</span>
                                </div>
                            </div>
                        </a>
                    </li>
                    <li class="option-with-dropdown">
                        <a href="{{ path('app_type', {type: 'node' }) }}">
                            <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                                <div class="flex items-center">
                                    <i class="fas fa-file-alt mr-2"></i>
                                    <span>Node</span>
                                </div>
                            </div>
                        </a>
                    </li>
                    <li class="option-with-dropdown">
                        <a href="{{ path('app_type', {type: 'bash' }) }}">
                            <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                                <div class="flex items-center">
                                    <i class="fas fa-file-alt mr-2"></i>
                                    <span>Bash Scripts</span>
                                </div>
                            </div>
                        </a>
                    </li>
                    <!-- ALWAYS LEAVE OTHER FIELD HERE -->
                    <li class="option-with-dropdown">
                        <a href="{{ path('app_type', {type: 'else' }) }}">
                            <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                                <div class="flex items-center">
                                    <i class="fas fa-file-alt mr-2"></i>
                                    <span>Other</span>
                                </div>
                            </div>
                        </a>
                    </li>
                    <!-- ALWAYS LEAVE EXES FIELD HERE -->
                    <li class="option-with-dropdown">
                        <a href="{{ path('app_exes') }}">
                            <div class="flex items-center justify-between p-2 hover:bg-gray-700">
                                <div class="flex items-center">
                                    <i class="fas fa-file-alt mr-2"></i>
                                    <span>Executables</span>
                                </div>
                            </div>
                        </a>
                    </li>
                {% endblock %} {# end sideBarPublic #}
                {% block sideBarAdmin %}
                    <!-- Add more links for the side navigation -->

                {% endblock %}
            </ul>
        </nav>
        <a href="https://leerlandais.com/portail"
           class="bottom-40 absolute bg-blue-500 py-2 px-6 mt-2 rounded-md text-white w-auto ml-12 font-bold cursor-pointer hover:bg-[#138808]">
            <button>
                Portfolio
            </button>
        </a>
    </aside>
{% endblock %} {# end sideBar #}
{% block footer %}
    <div class="flex place-self-center my-20  align-middle">
        &copy; <a href="https://leerlandais.com" target="_blank">Leerlandais 2024</a>
    </div>
{% endblock %}


{% block javascript %}
    <script src="{{ asset('scripts/sideBarScript.js') }}"></script>
{% endblock %}